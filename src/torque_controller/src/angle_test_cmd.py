#!/usr/bin/eng python

import numpy as np
import math

def angle():
    q_cmd = 0

def langles():
    q_cmd = np.array([0, 0, 0, 0, 0, 0, 0])
    q_cmd = q_cmd.tolist()
    return q_cmd

def rangles():
    q_cmd = np.array([0, 0, 0, math.pi/6.0, 0, 0, 0])
    q_cmd = q_cmd.tolist()
    return q_cmd

def angles():
    q_cmd = np.array([0, 0, 0, 0, 0, 0, 0])
    q_cmd = q_cmd.tolist()
    return q_cmd
